box: wercker/nodejs
build:
  steps:
    - script:
        name: install dependencies
        code: |-
            npm install --production 
    - script:
        name: grunt run
        code: |-
            grunt compile     
deploy:
  steps:
    - sjoerdmulder/rsync-deploy@0.1.0:
        host: dash-flippi.rhcloud.com
        user: 544230665973caed8a000622
        directory: $OUTDIR
        sshkey: $SSH_PRIVATE      
